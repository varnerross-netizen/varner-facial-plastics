Place the practice assets here using these filenames:

- `logo.png`
- `dr-varner-suit.jpg`
- `dr-varner-scrubs.jpg`

The site already references the portrait files and will show polished fallback panels until the real photos are added.
